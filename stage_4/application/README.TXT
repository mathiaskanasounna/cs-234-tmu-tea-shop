========================
BUILD OUTPUT DESCRIPTION
========================

To run the project, first download the application folder and run the command:

java -jar "Shop.jar" 

The data are locate in the folder src/data

To compile the project you will need to use Netbeans Product Version: Apache NetBeans IDE 14.
The Netbeans project is in the archive Shop-Netbeans-Project.zip